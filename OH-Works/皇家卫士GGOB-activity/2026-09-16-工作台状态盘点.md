# 工作台状态盘点

日期：2026-09-16 21:32（心跳巡检）

## 一句话结论
工作台自 2026-07-25 起未再变动，本轮无需用户介入；现有新增文件是 6/22–7/25 期间项目工作的遗留产物，此处仅做事实记录，不移动、不删除任何文件。

## 文件构成（工作台根目录）
按最后修改时间回溯，根目录约 130 个散落文件，可分四类：

1. **receipt-ocr-service/**（完整子项目，最后改动 7 月上半月）
   - FastAPI 服务：`app/main.py`、`app/extractor.py`、`app/ocr_engine.py`、`app/preprocess.py`、`app/schemas.py`
   - 部署件：`Dockerfile`、`docker-compose.yml`、`requirements.txt`、`config.yaml`
   - OCR 选型：`config.yaml` 中 `lang_list: [ch_sim, en]`、CPU 模式、单 worker（注释说明 PaddleOCR 非线程安全）
   - 测试样图：`test_receipt*.png`（3 张）

2. **Dify 工作流相关**（改动集中在 7 月初）
   - 工作流定义：`HR简历筛选助手.yml`、`HR.yml`、`receipt-ocr-workflow.yml`、`fixed_workflow.yml`、`original.yml`
   - 部署/修复脚本：`deploy_workflow.py`、`import_direct.py`、`rebuild_wf.py`、`restore_workflow.py`、`restore_from_yml.py`、`expand_workflow.py`，以及大量 `fix_*.py`
   - 数据库/账号脚本：`create_app_db.py`、`add_account.py`、`reset_pwd.py`、`do_reset.py`、`direct_reset.py`
   - 图谱/DSL 中间产物：`graph.json`、`_wf_graph.json`、`fixed_workflow_graph.json`、`dsl_b64.txt`、`dsl_loader.js`、`import_payload.json`

3. **网页抓取/登录会话**（最后改动 7/25，是根目录最新的一批）
   - `qpyun_scraper.py`、`parse_menu.py`、`menu_urls.json`、`home_full.html`、`home_static.html`
   - 调试件：`debug1_after_switch.png`、`debug2_after_fill.png`、`debug3_final.png`、`test_login.py`、`test_login2.py`、`qpyun_cookies.txt`

4. **环境与索引**
   - `langfuse/`、`crawl-api-mock/`、`node_modules/`、`codex-skills/`
   - `SKILL_INDEX.md`、`python3.cmd`、`copy_python3.bat`、`env_vars.json`

## 需要留意的两点（仅提示，未处置）
1. 根目录存在 `qpyun_cookies.txt`（含登录会话 Cookie）与 `env_vars.json`。当前工作台**不是** Git 仓库（无 `.git`），因此暂无提交外泄风险；若日后纳入版本控制，需先把这类文件加入忽略清单。
2. `fix_*.py` / `restore_*.py` / `_temp_*.py` 一类一次性脚本数量可观，属可归档范畴，但归属与去留应由你本人判断，我不擅自移动。

## 说明
- 本次盘点为只读分析 + 记录，未执行任何写操作（除本笔记）。
- 上一次工作台变动记录为 `2026-06-18-Workspace-Changes.md`，其后再无记录；本文覆盖 6/22–7/25 的增量。
