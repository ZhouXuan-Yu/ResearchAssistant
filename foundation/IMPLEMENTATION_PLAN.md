# IMPLEMENTATION_PLAN.md

| 项 | 值 |
|---|---|
| 阶段 | Foundation Stage（只建设基础设施，不开展正式科研） |
| 制定时间 | 2026-09-16 21:49 (+08:00) |
| 制定者 | 科研助手（agent-mu42qzfz） |
| 当前进度 | F0 已完成，等待用户审查与授权进入 F1 |
| 总原则 | 每个阶段独立验收；前一阶段未通过验收，不得进入下一阶段 |

## 执行前置约定

1. 每个阶段的启动都需要用户**显式授权**；本计划不代表已获授权。
2. 每个阶段开始前建立回滚点，结束后记录变更日志。
3. 涉及「安装/覆盖/删除/定时任务/多 Agent」的动作，一律先取得用户确认。
4. 全阶段遵守：不输出凭据内容、不开展正式科研、不做秦简文献检索。
5. 任一阶段可被用户中止，并按回滚点复原。

---

## F0 环境审计

**状态：已完成**（见 `ENVIRONMENT_AUDIT.md`）

| 项 | 内容 |
|---|---|
| 目标 | 摸清平台、运行时、模型、记忆、技能、权限、网络、运维能力的真实状态 |
| 动作 | 只读采集配置与目录；逐项标注状态（平台支持/已经配置/尚未配置/无法确认） |
| 产出 | `ENVIRONMENT_AUDIT.md` |
| 验收标准 | 审计项全覆盖 12 个范围；无法核验项均标「待确认」；无任何写操作 |
| 遗留 | 13 项待确认（见报告 §13） |

---

## F1 备份与回滚

| 项 | 内容 |
|---|---|
| 目标 | 在任何后续变更前，建立可复原的基线快照与回滚流程 |
| 前置 | F0 通过 |
| 动作 | ① 快照 `config.yaml`、`preferences.json`、`models.json`、`provider-catalog.json`、`skills.enabled`；② 记录 HanaAgent 版本对（桌面/服务端）；③ 定义回滚 SOP（文件级/配置级/技能级）；④ 明确备份保留策略 |
| 产出 | `BACKUP-MANIFEST.md`、快照存档目录、回滚 SOP |
| 验收标准 | 能从快照复原任一已登记文件；回滚步骤经一次演练验证（演练仅针对自建副本） |
| 回滚 | 快照本身即为回滚源 |
| 风险 | 备份保留期短（当前 `retention_days=1`）；需评估是否延长 |
| 授权点 | 需要写权限在 `OH-WorkSpace` 下创建快照目录 |

---

## F2 项目目录和记录模板

| 项 | 内容 |
|---|---|
| 目标 | 建立与研究方向无关的目录骨架与记录模板，供 F8 之后直接使用 |
| 前置 | F1 通过 |
| 动作 | ① 设计目录骨架（数据/标注/评测/知识/实验/写作/治理）；② 生成记录模板（数据登记表、实验日志、决策记录 ADR、变更日志、引用登记表） |
| 产出 | 目录骨架 + 模板文件集 |
| 验收标准 | 模板可被填充且字段齐全（含来源、时间、版本、许可、置信度）；目录无空壳占位遗留 |
| 回滚 | 目录可整体删除（不触碰既有文件） |
| 授权点 | 目录与模板的具体位置需用户确认 |

---

## F3 首批核心 Skills 逐个安装测试

| 项 | 内容 |
|---|---|
| 目标 | 以最小集验证技能安装、依赖、权限与可用性，形成「逐个安装 + 单独测试」的稳定流程 |
| 前置 | F1、F2 通过 |
| 动作 | ① 从候选中选定首批（建议仅 2–3 个只读类）；② 逐个安装；③ 每个技能执行一次最小用例测试；④ 记录来源/提交/许可证/依赖/权限 |
| 产出 | 安装测试记录；更新 `SKILLS-GOVERNANCE.md` |
| 验收标准 | 每个技能通过最小用例；无依赖缺失；可单独卸载且不留残余 |
| 回滚 | 卸载技能目录 + 从 `skills.enabled` 移除 |
| 授权点 | 候选清单与首批范围需用户确认（不得批量安装） |

---

## F4 Skills 路由和冲突测试

| 项 | 内容 |
|---|---|
| 目标 | 厘清技能触发边界，验证编排/路由层可用 |
| 前置 | F3 通过 |
| 动作 | ① 对重叠组（见 `ENVIRONMENT_AUDIT.md` §11）做触发测试；② 设计并验证路由规则（含 `research-os-router` 的需求规格，若用户批准自建）；③ 记录误触发与漏触发案例 |
| 产出 | 路由规则草案、冲突测试报告、`research-os-router` 需求规格（或替代方案） |
| 验收标准 | 重叠组内触发可区分；无不可解释的误触发 |
| 回滚 | 路由规则文件可回退 |
| 授权点 | 如需自建路由技能，须用户批准后再进入安装 |

---

## F5 多 Agent 职责隔离

| 项 | 内容 |
|---|---|
| 目标 | 若引入多 Agent，确保职责、权限、技能集相互隔离 |
| 前置 | F4 通过 |
| 动作 | ① 定义 Agent 角色与职责边界；② 定义各 Agent 技能集与权限档位；③ 验证互不越权 |
| 产出 | 多 Agent 职责矩阵 |
| 验收标准 | 任一 Agent 无法访问超出其职责的技能/权限 |
| 回滚 | 新建 Agent 可停用/移除（不影响既有 Agent） |
| 授权点 | **本阶段约束明确禁止创建多 Agent**；F5 仅可制定方案，创建须另行授权 |

---

## F6 周检与月度同质化巡检

| 项 | 内容 |
|---|---|
| 目标 | 建立可持续的技能治理巡检机制 |
| 前置 | F4、F5 就绪 |
| 动作 | ① 配置每周只读更新检查（发现更新只报告）；② 配置每月同质化/触发冲突/权限变化检查；③ 定义季度回归基准 |
| 产出 | 巡检任务定义与报告模板 |
| 验收标准 | 巡检可产出结构化报告；无法检查上游时标记「检查失败」 |
| 回滚 | 停用巡检任务 |
| 授权点 | **本阶段约束明确禁止启动定时任务**；F6 仅可制定方案，启动须另行授权 |

---

## F7 端到端验收

| 项 | 内容 |
|---|---|
| 目标 | 对基础设施做一次完整验收，确认可支撑正式科研 |
| 前置 | F1–F6 通过 |
| 动作 | ① 走通「获取资料 → 解析 → 记录 → 治理登记」闭环（用非秦简的公开材料）；② 验证记忆/回滚/巡检/权限各环节；③ 逐条复核 F0 的 13 项待确认 |
| 产出 | 端到端验收报告；待确认清单的结项结论 |
| 验收标准 | 闭环可复现；待确认项全部给出结论或明确转为已知限制 |
| 回滚 | 按 F1 快照复原 |
| 授权点 | 验收所用材料需用户确认 |

---

## F8 等待用户确定研究方向

| 项 | 内容 |
|---|---|
| 目标 | 基础设施就绪后，等待用户确定正式研究方向，再开展科研 |
| 前置 | F7 通过 |
| 动作 | 不主动开展科研；等待用户输入方向、目标期刊与约束 |
| 产出 | 研究方向确认单（由用户填写） |
| 验收标准 | 用户明确方向后方可进入科研阶段 |
| 说明 | **本阶段不进行秦简文献检索、不建科研项目、不写论文** |

---

## 阶段依赖与授权总览

```
F0(完成) → F1 → F2 → F3 → F4 → F5(仅方案) → F6(仅方案) → F7 → F8(等待)
                                    ↑              ↑
                              需另行授权创建    需另行授权启动
```

## 当前停止点

F0 已完成。**本计划不自动推进**。等待用户审查 `ENVIRONMENT_AUDIT.md`、本文件与 `RISK_REGISTER.md`，并显式授权后，方可开始 F1。
