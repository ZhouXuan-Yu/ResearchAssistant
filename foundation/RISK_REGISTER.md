# RISK_REGISTER.md

| 项 | 值 |
|---|---|
| 阶段 | Foundation Stage |
| 生成时间 | 2026-09-16 21:49 (+08:00) |
| 生成者 | 科研助手（agent-mu42qzfz） |
| 依据 | `ENVIRONMENT_AUDIT.md` 的只读事实，不含任何凭据内容 |
| 说明 | 未经验证的一律标注「待确认」，不作为已成立风险处理 |

## 等级与状态定义

- **等级**：高 / 中 / 低（= 可能性 × 影响）。
- **状态**：`待确认`（证据不足）、`已确认`（有直接证据）、`已缓解`。
- **处置阶段**：建议在哪个 F 阶段处理。

---

## 风险清单

| ID | 风险 | 类别 | 证据 | 可能性 | 影响 | 等级 | 建议应对 | 状态 | 处置阶段 |
|---|---|---|---|---|---|---|---|---|---|
| R-01 | 记忆模型来源歧义：Agent 级 `utility_large=""`、全局 `utility_large_model=gpt-5.6-sol`，实际生效值不明 | 配置一致性 | `config.yaml` vs `preferences.json` | 中 | 中 | 中 | F1 时确定解析优先级并显式固化 | 待确认 | F1 |
| R-02 | 视觉辅助模型已配置（`gpt-5.6-luna`）但出现 `fetch failed`，图像理解不可用 | 功能可用性 | `logs`（多次 fetch failed） | 高 | 中 | 高 | 核实 provider 通道与凭据；必要时切换本地视觉模型 | 已确认 | F1 |
| R-03 | 嵌入模型与 provider 均为空，语义/向量记忆可能不可用 | 功能缺失 | `config.yaml`（`models.embedding=""`、`embedding_api.provider=""`） | 高 | 中 | 高 | 明确是否需要语义记忆；如需要则配置嵌入（本机有 `bge-m3`） | 待确认 | F1/F4 |
| R-04 | 服务端以 `lan` 模式监听 `0.0.0.0:14500`，局域网内可达 | 安全边界 | `server-network.json` | 中 | 高 | 高 | 评估是否改为 loopback；核查访问令牌与主机防火墙 | 已确认 | F1 |
| R-05 | 桌面端(0.407.15)与服务端(0.412.7)版本不一致 | 环境一致性 | `last-update-version` vs `server-info.json` | 中 | 中 | 中 | 确认是否预期；如非预期，统一版本 | 待确认 | F1 |
| R-06 | 高权限项同时开启：`allow_full_access_plugins=true`、`plugin_dev_tools.enabled=true` | 安全边界 | `preferences.json` | 中 | 高 | 高 | 评估是否可关闭；F3 安装技能前先降权 | 已确认 | F1 |
| R-07 | 桥接开关不一致：全局 `channels_enabled=true`、Agent 级 `false` | 配置一致性 | `preferences.json` vs `config.yaml` | 中 | 低 | 低 | 明确以哪一层为准 | 待确认 | F1 |
| R-08 | 备份留存短：`retention_days=1`、`max_file_size_kb=1024` | 可恢复性 | `preferences.json` | 高 | 中 | 中 | F1 建立独立快照目录，不依赖默认留存 | 已确认 | F1 |
| R-09 | 无统一的配置/技能整体快照与一键回滚机制 | 可恢复性 | 目录证据（仅零散 backups） | 中 | 中 | 中 | F1 建立快照清单与回滚 SOP | 已确认 | F1 |
| R-10 | 技能基数大（284 个 SKILL.md / 141 启用），触发冲突面广 | 治理 | 技能目录与 `skills.enabled` | 高 | 中 | 中 | F4 做重叠组触发测试；F6 月度巡检 | 已确认 | F4/F6 |
| R-11 | `server-info.json` 含明文访问令牌 | 凭据安全 | `server-info.json`（值未读取输出） | 低 | 中 | 中 | 确认该文件权限；确保不进入版本库或共享 | 已确认 | F1 |
| R-12 | 沙箱账户对工作区与 `.hanako` 具读+执行权限 | 权限 | `Get-Acl` 输出 | 低 | 低 | 低 | 保持现状；后续新增目录沿用同策略 | 已确认 | F2 |
| R-13 | Python 3.14 较新，部分科研依赖可能无预编译 wheel | 依赖兼容 | `python --version` | 中 | 中 | 中 | F3 安装含 Python 依赖的技能时逐个验证 | 待确认 | F3 |
| R-14 | 第三方技能供应链：`nature-skills` 含商业推广内容；ARS 为 CC-BY-NC-4.0 | 供应链/许可 | 上游 README、LICENSE | 中 | 中 | 中 | 保持固定提交与只读更新检查；商业用途前复核许可 | 已确认 | F3/F6 |
| R-15 | 存在 1 个空 prompt 的禁用定时任务（`studio_job_2`），若被误启用会空跑 | 运维 | `automation list` | 低 | 低 | 低 | F6 前先复核该任务，决定是否清理（需授权） | 已确认 | F6 |
| R-16 | 记忆编译曾因模型不可用连续失败，健康状态可能残留 | 功能可用性 | `logs`（21:05–21:06 fetch failed） | 中 | 低 | 低 | F1 复核记忆编译在修复后是否恢复 | 已确认 | F1 |
| R-17 | `academic-research-suite` 为空壳技能（仅 1 个 SKILL.md），可能被误触发 | 治理 | 技能目录 | 中 | 低 | 低 | F4 决定替换（装 ARS 完整管线）或停用 | 已确认 | F4 |
| R-18 | 信息缺口：13 项「待确认」（版本/记忆/视觉/嵌入/沙箱语义/日志策略/凭据形态/回滚能力） | 认知 | `ENVIRONMENT_AUDIT.md` §13 | 高 | 中 | 中 | 在 F1、F7 逐项结项 | 待确认 | F1/F7 |
| R-19 | 记忆/配置改动历史表明曾发生模型不可用导致连锁失败，缺乏事前校验 | 运维 | 日志时间线 | 中 | 中 | 中 | F1 增加「变更前校验模型可用性」步骤 | 已确认 | F1 |

---

## 高等级风险摘要（需优先处理）

1. **R-02 视觉模型不可用**（高）——已确认，影响任何图像类任务。
2. **R-03 嵌入/语义记忆缺失**（高）——待确认，影响知识型记忆。
3. **R-04 服务端 LAN 暴露**（高）——已确认，安全边界问题。
4. **R-06 高权限插件项开启**（高）——已确认，应在安装技能前收敛。

## 与待办阶段的映射

- **F1（备份与回滚）**：R-01、R-02、R-03、R-04、R-05、R-06、R-08、R-09、R-11、R-16、R-18、R-19。
- **F3（技能安装测试）**：R-13、R-14。
- **F4（路由与冲突）**：R-03、R-10、R-17。
- **F6（巡检）**：R-10、R-14、R-15。
- **F7（端到端验收）**：R-18 结项。

---

## 免责

本清单所有条目均来自只读证据；标「待确认」者未经验证，不得作为已成立结论使用。本阶段不执行任何风险处置动作。
